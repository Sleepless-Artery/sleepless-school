apiVersion: v1
kind: Service
metadata:
  name: postgres-submissions
  namespace: online-learning-platform
  labels:
    app: postgres-submissions
spec:
  ports:
    - port: 5432
      name: postgres-submissions
  clusterIP: None
  selector:
    app: postgres-submissions