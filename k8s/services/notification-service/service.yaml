apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: online-learning-platform
spec:
  selector:
    app: notification-service
  ports:
    - port: 80
      targetPort: 8086
      protocol: TCP
