apiVersion: v1
kind: Service
metadata:
  name: postgres-users
  namespace: online-learning-platform
  labels:
    app: postgres-users
spec:
  ports:
    - port: 5432
      name: postgres-lessons
  clusterIP: None
  selector:
    app: postgres-users