apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: online-learning-platform
spec:
  selector:
    app: user-service
  ports:
    - name: http
      port: 80
      targetPort: 8082
    - name: grpc
      port: 9093
      targetPort: 9093
  type: ClusterIP
