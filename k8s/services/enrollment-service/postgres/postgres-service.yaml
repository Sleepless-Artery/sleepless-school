apiVersion: v1
kind: Service
metadata:
  name: postgres-enrollments
  namespace: online-learning-platform
  labels:
    app: postgres-enrollments
spec:
  ports:
    - port: 5432
      name: postgres-enrollments
  clusterIP: None
  selector:
    app: postgres-enrollments