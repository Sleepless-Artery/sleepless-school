apiVersion: v1
kind: Service
metadata:
  name: postgres-courses
  namespace: online-learning-platform
  labels:
    app: postgres-courses
spec:
  ports:
    - port: 5432
      name: postgres-courses
  clusterIP: None
  selector:
    app: postgres-courses