apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: online-learning-platform
spec:
  selector:
    app: gateway-service
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
