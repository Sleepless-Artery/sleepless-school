server:
  port: 8081

spring:
  datasource:
    url: ${AUTH_DB_URL}
    username: ${AUTH_DB_USERNAME}
    password: ${AUTH_DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 30000
      initialization-fail-timeout: 60000
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    encoding: UTF-8
    validate-on-migrate: true
    baseline-on-migrate: true
    connection-retries: 10
    init-sqls: "SELECT 1"

  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
    producer:
      bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      retries: 5
      acks: all
      batch-size: 65536
      delivery-timeout-ms: 30000
      linger-ms: 1000
      request-timeout-ms: 10000
      retry-backoff-ms: 1000
    consumer:
      bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
      group-id: auth-service
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.ErrorHandlingDeserializer
      auto-offset-reset: earliest
      enable-auto-commit: false
      concurrency: 1
      trusted-packages: org.sleepless_artery.auth_service.*
    topic:
      prefix: auth
      domain: users
      partitions: 1
      replicas: 1
      minInsyncReplicas: 1

  cache:
    type: redis
  data:
    redis:
      host: ${AUTH_REDIS_HOST:}
      port: ${AUTH_REDIS_PORT:6379}
      password: ${AUTH_REDIS_PASSWORD:}

jwt:
  secret: ${JWT_SECRET:default-secret-key}
  lifetime: ${JWT_LIFETIME:6h}

admin:
  email-address: ${ADMIN_EMAIL_ADDRESS}
  password: ${ADMIN_PASSWORD}

basic-request-path: auth


grpc:
  server:
    port: 9091
  client:
    GLOBAL:
      negotiation-type: plaintext
      keep-alive-time: 30s
      keep-alive-timeout: 20s
    user-service:
      address: ${USER_SERVICE_GRPC_URL:static://user-service:9093}


resilience4j:
  circuitbreaker:
    instances:
      user-service:
        sliding-window-type: COUNT_BASED
        sliding-window-size: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s
        minimum-number-of-calls: 5
        permitted-number-of-calls-in-half-open-state: 3
        automatic-transition-from-open-to-half-open-enabled: true
        register-health-indicator: false

  retry:
    instances:
      user-service:
        max-attempts: 1
        wait-duration: 300ms
        retry-exceptions:
          - io.grpc.StatusRuntimeException
          - org.sleepless_artery.auth_service.common.exception.ExternalServiceUnavailableException

management:
  endpoints:
    web:
      exposure:
        include: health
      base-path: /internal/actuator
  endpoint:
    health:
      probes:
        enabled: true
      group:
        readiness:
          include: db, diskSpace
        liveness:
          include: ping


logging:
  level:
    org:
      springframework: WARN
      hibernate:
        SQL: WARN
        type.descriptor.sql.BasicBinder: WARN
      sleepless_artery:
        auth_service: WARN


springdoc:
  api-docs:
    path: /internal/openapi
  swagger-ui:
    path: /internal/swagger
    operationsSorter: method
    tagsSorter: alpha
    display-request-duration: true
  show-actuator: false
  packages-to-scan:
    - org.sleepless_artery.auth_service.authentication.controller
    - org.sleepless_artery.auth_service.email.controller
    - org.sleepless_artery.auth_service.password.controller
    - org.sleepless_artery.auth_service.registration.controller